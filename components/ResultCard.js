export function renderResultCard(title, body) {
  return `<article class="ncm-card"><div class="ncm-card-header"><span class="ncm-code">${title}</span></div><div class="section">${body}</div></article>`;
}
